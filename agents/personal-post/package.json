{
  "name": "personal-post",
  "version": "0.2.0",
  "private": true,
  "description": "Runtime dependencies for the personal-post export and visual QA scripts.",
  "type": "module",
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "scripts": {
    "postinstall": "echo 'Now run: npx playwright install chromium'"
  }
}
